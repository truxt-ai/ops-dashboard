'use strict';

/* Stub — replaced by the implementer. All functions return zero-values so
   tests compile-and-fail rather than error at require-time. */

const PROMO_CODES = [];

function applyPromoCode(price, code) {
  return 0;
}

function listActivePromoCodes() {
  return [];
}

module.exports = { PROMO_CODES, applyPromoCode, listActivePromoCodes };
