'use strict';

// Stub — implementation not yet written.
// Returns zero/empty values so tests compile and fail (not skip).

function applyPromoCode(price, code) {
  return undefined;
}

function getActivePromoCodes() {
  return [];
}

module.exports = { applyPromoCode, getActivePromoCodes };
