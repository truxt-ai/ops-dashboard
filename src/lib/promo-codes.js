'use strict';

// Stub: returns zero values so tests are RED until the implementation lands.
function listActivePromoCodes() { return []; }
function applyPromoCode(code, price) { return 0; }

module.exports = { listActivePromoCodes, applyPromoCode };
