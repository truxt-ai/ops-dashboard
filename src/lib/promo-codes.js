'use strict';

// STUB — returns wrong sentinel values so tests are RED until implementation lands.

function listActivePromoCodes() {
  return [];
}

function applyPromoCode(price, _code) {
  return 0;
}

module.exports = { listActivePromoCodes, applyPromoCode };
